<?php



use Launchpad\Dependencies\LaunchpadCore\Container\Options;

class Subscriber {
	/**
	 * @var Options
	 */
	protected $options;

	/**
	 * @param Options $options
	 */
	public function __construct( Options $options, int $test ) {
		$this->options = $options;
	}


}

class AdminSubscriber {
	/**
	 * @var Options
	 */
	protected $options;

	/**
	 * @param Options $options
	 */
	public function __construct( Options $options, int $test ) {
		$this->options = $options;
	}
}

class SecondSubscriber {
	/**
	 * @var Options
	 */
	protected $options;

	/**
	 * @param Options $options
	 */
	public function __construct( Options $options, int $test ) {
		$this->options = $options;
	}
}

class ThirdSubscriber {
	/**
	 * @var Options
	 */
	protected $options;

	/**
	 * @param Options $options
	 */
	public function __construct( Options $options, int $test ) {
		$this->options = $options;
	}
}

class FourthSubscriber {
	/**
	 * @var Options
	 */
	protected $options;

	/**
	 * @param Options $options
	 */
	public function __construct( Options $options, int $test ) {
		$this->options = $options;
	}
}

class FifthSubscriber {
	/**
	 * @var Options
	 */
	protected $options;

	/**
	 * @param Options $options
	 */
	public function __construct( Options $options, int $test ) {
		$this->options = $options;
	}
}

class SixSubscriber {
	/**
	 * @var Options
	 */
	protected $options;

	/**
	 * @param Options $options
	 */
	public function __construct( Options $options, int $test ) {
		$this->options = $options;
	}
}

class SevenSubscriber {
	/**
	 * @var Options
	 */
	protected $options;

	/**
	 * @param Options $options
	 */
	public function __construct( Options $options, int $test ) {
		$this->options = $options;
	}
}

class HeightSubscriber {
	/**
	 * @var Options
	 */
	protected $options;

	/**
	 * @param Options $options
	 */
	public function __construct( Options $options, int $test ) {
		$this->options = $options;
	}
}

class NineSubscriber {
	/**
	 * @var Options
	 */
	protected $options;

	/**
	 * @param Options $options
	 */
	public function __construct( Options $options, int $test ) {
		$this->options = $options;
	}
}